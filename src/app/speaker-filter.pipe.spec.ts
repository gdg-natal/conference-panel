import { SpeakerFilterPipe } from './speaker-filter.pipe';

describe('SpeakerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeakerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
